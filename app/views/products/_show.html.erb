	<div class="products">
	<% index_products.each do |f| %>
		<%= link_to product_path(f) do %>
		<div class="products">
			<dl>
				<dt>商品名</dt>
					<dd><%= f.name %></dd>
				<dt>価格</dt>
			</dl>
		</div>
		<% end %>
	<% end %>
	</div>