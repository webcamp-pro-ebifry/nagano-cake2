
<div class="container products_index">
<% provide(:title, "商品一覧") %>
	<div class="row">
		<div class="sidebar col-lg-2">
			<%= render 'sidebar', genres: @genres %>
		</div>

		<div class="col-lg-10">
		<h1 style="font-weight: bold">
		<% if @genre.present? == true %>
			<%= @genre.name %><span class="count">
			（全<%= @quantity %>件）
		<% else %>
		<h1 style="font-weight: bold">商品一覧<span class="count"></span></h1>
		（全<%= @quantity %>件）
		</span>

		<%= render "show", product: @product, index_products: @products %>
        <p class="pagenation"><%= paginate @products %></P>

			<% end %>

		</div>
	</div>
</div>